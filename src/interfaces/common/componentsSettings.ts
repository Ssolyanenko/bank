export enum ButtonType {
  SUBMIT = 'submit',
  RESET = 'reset',
  BUTTON = 'button',
}

export enum InputType {
  TEXT = 'Login',
  PASSWORD = 'Password',
  CHECHBOX = 'Checkbox',
}

export enum Size {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}
